export enum FunctionType {
  /**
   * send text message
   */
  sendText = 'sendText',
  /**
   * send audio in base64
   */
  sendAudioBase64 = 'sendAudioBase64',
  /**
   * send an audio message
   */
  sendAudio = 'sendAudio',
  /**
   * send file message
   */
  sendFile = 'sendFile',
  /**
   * send image file
   */
  sendImage = 'sendImage',
}
